# Сервис формирования цифрового признака ТС

## Go-бэкенд (Spec First)

Контракт API, Go-код и инструкции запуска находятся в [`backend/`](backend/README.md).
Источник истины: [`backend/api/openapi.yaml`](backend/api/openapi.yaml).
Генерация: `cd backend`, затем `go generate ./...`.
Реализованы загрузка кадров, галерея, извлечение признаков и поиск через PostgreSQL/pgvector,
MinIO и отдельный Python ML-сервис. [Инструкция для фронтенда](backend/FRONTEND.md).

Полный запуск: `docker compose up -d --build`. API: `http://localhost:8080`,
контракт: `http://localhost:8080/openapi.json`, готовность: `http://localhost:8080/readyz`.

Этот раздел репозитория отвечает за извлечение векторного представления (эмбеддинга) автомобиля по изображению и координатам BBox.

## Что реализовано

* **Модель:** Архитектура ResNet50, обученная с использованием функции потерь ArcFace. Это позволяет формировать устойчивые кластеры в латентном пространстве и корректно работать в условиях Open-set (идентифицировать автомобили, которых не было в обучающей выборке).
* **Режим отказа:** Используется порог косинусного сходства 0.36. Скрипт подбора максимизирует F1-score на парах обучающих данных; качество на независимой выборке требует отдельной проверки.
* **API-ready модуль:** Процесс обрезки изображения и генерации вектора вынесен в изолированный класс `VehicleFeatureExtractor` для мгновенной интеграции с бэкендом.

## Структура файлов

* `core/` — продакшен-код для сборки микросервиса. Включает скрипт инференса и обученные веса (`weights/`). Эту папку использует бэкенд.
* `expeiments/` — исследовательская среда. Содержит скрипты парсинга BBox, тренировочный цикл и код расчета порога.
* `requirements.txt` — зависимости исследовательских скриптов без фиксации версий.
* `core/requirements-serving.txt` — версии зависимостей ML-сервиса для Docker-образа.

## Локальная инфраструктура

`docker-compose.yml` запускает Go API, Python ML, Postgres + pgvector и S3-совместимое хранилище MinIO.

1. Скопируйте переменные окружения:

   ```bash
   cp .env.example .env
   ```

2. Запустите инфраструктуру:

   ```bash
   docker compose up -d --build
   ```

3. Подключение из Go-бэкенда:

   * Postgres: `postgres://brightest_teeth:brightest_teeth@localhost:5432/brightest_teeth?sslmode=disable`
   * S3 endpoint: `http://localhost:9000`
   * S3 endpoint из контейнеров Compose: `http://minio:9000`
   * S3 bucket: `vehicle-photos`
   * MinIO console: `http://localhost:9001`

При первом старте Postgres автоматически включает расширение `vector` и создает таблицы `vehicle_photos` и `vehicle_observations`. Эмбеддинг модели хранится как `vector(512)` и индексируется через HNSW для cosine-поиска.

